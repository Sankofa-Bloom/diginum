# DigiNum Frontend Environment Variables
# These values are exposed to the browser and should be safe to expose

# API Configuration
# API URL is automatically determined by config.ts based on environment

# Supabase (Public keys only)
VITE_SUPABASE_URL=https://your-supabase-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Swychr Payment Gateway (Public configuration)
VITE_SWYCHR_ENABLED=true
VITE_SWYCHR_BASE_URL=https://api.accountpe.com/api/payin

# Note: All sensitive credentials are stored in backend/.env only
